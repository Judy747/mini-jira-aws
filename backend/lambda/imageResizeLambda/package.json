{
  "name": "mini-jira-image-resize-lambda",
  "version": "1.0.0",
  "private": true,
  "description": "S3-triggered thumbnail generator (deploy separately from the Express API)",
  "main": "index.js",
  "scripts": {
    "zip": "npm ci --omit=dev && zip -r ../imageResizeLambda.zip . -x '*.git*'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.787.0",
    "sharp": "^0.33.5"
  }
}
